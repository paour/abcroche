# Copy to .env for `npm run dev` (it's gitignored).

# Act as if the sign-in proxy had signed this user in, so "Save online" and
# the online song list work locally. Never set this in production.
DEV_USER=dev

# PORT=8000
# PUBLIC_URL=http://localhost:8000
